name: daily-intake
on:
  schedule: [{cron: "20 11 * * *"}]   # 06:20 CT daily
  workflow_dispatch: {}
jobs:
  intake:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - run: pip install -e engine[dev]
      - run: echo "TODO wire btw_engine.watch → fetch → extract → normalize → review PR"
      # secrets: SUPABASE_URL, SUPABASE_KEY, R2_*, ANTHROPIC_API_KEY, GH_BOT_TOKEN
