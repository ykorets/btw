id: gas-nominations
kind: api
url: "https://www.gasquest.com/informational-posting"  # Boardwalk GasQuest (ex-infopost.bwpipelines.com)
query: "TGT (TSP 100000) operationally-available / scheduled quantities, location ~ 'MZX'"
schedule: daily
slo_interval_days: 14
parser: gas-ebb_v1
status: recon_done  # IW-v1 recon 2026-07-12 (docs/island-watch/gas-recon-2026-07.md):
                    # SOUTHAVEN C2 — capturable: Texas Gas Transmission (Boardwalk,
                    # FERC TSP 100000), delivery meter "MZX Southaven" (MZX Tech,
                    # DeSoto Co MS) confirmed present in daily postings via a
                    # third-party flow tracker (Feb 2026). Capture: daily OA/
                    # scheduled-quantities per NAESB cycle -> filter location name
                    # contains 'MZX' -> flow value = operation pulse. Parser TODO:
                    # probe GasQuest (JS app) for the CSV/JSON endpoint behind the
                    # UI before considering headless.
                    # ABILENE — no public pulse (honest limit): private lateral
                    # (Primoris/Longhorn) + planned private 42" Permian line;
                    # intrastate candidates (Atmos Pipeline-Texas, ONEOK WesTex)
                    # have no FERC EBB obligation. Pulse stays satellite-first.
