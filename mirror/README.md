# Data mirror (placeholder)
The generated read-only export lives in its own repo: `behindthewatt/data`.
Never hand-edited; regenerated from Postgres on every publish (decisions.md D1/D4).
This folder only documents the export format during development.
